## Summary

<!-- What does this PR do, and why? -->

## Changes

-

## Testing

<!-- How was this verified? Commands run, manual steps, screenshots. -->

## Checklist

- [ ] Backend builds (`go build ./...`) and vets clean (`go vet ./...`)
- [ ] Frontend builds (`npm run build`) and lints clean (`npm run lint`)
- [ ] No secrets committed (`.env` / `.env.local` stay git-ignored)
- [ ] Docs updated if behavior or config changed (README / DEPLOYMENT)
